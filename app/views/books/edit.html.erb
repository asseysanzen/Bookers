<h2>Editing Book</h2>
<%= form_for(@book) do |f| %>
    <p>Title</p>
    <%= f.text_field :title %>
    <p>Body</p>
    <%= f.text_area :body %>
    <%= f.submit 'Update book' %>
<% end  %>
<%= link_to "Show", book_path(@book.id) %>
<%= link_to "Back", '/books' %>